#ifndef ASSIGNMENT_2__BOOKSTORE_MANAGER_H_
#define ASSIGNMENT_2__BOOKSTORE_MANAGER_H_

#include "book.h"

class BookstoreManager {
public:
	/**
	 * @return true if the list is empty
	 */
//	bool isEmpty();
	
	/**
	 * @return true if the underlying array is full
	 */
//	bool isFull();
	
	/**
	 * Prints the number of books in this list.
	 */
//	void listSize();
	
	/**
	 * Prints this list.
	 */
//	void print();
	
	/**
	 * Pushes book to this list.
	 * @param book 
	 */
//	void insert(Book book);
	
	/**
	 * Removes all books with the same ISBN as given book.
	 * @param book 
	 */
//	void remove(Book book);
	
	/**
	 * Removes all books with the given publisher.
	 * @param publisher 
	 */
//	void removePublisher(std::string publisher);
	
	/**
	 * Prints the contents of given book.
	 * @param book 
	 */
//	void search(Book book);
};

#endif //ASSIGNMENT_2__BOOKSTORE_MANAGER_H_
